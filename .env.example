# Airtable — server-side only, never expose to the browser.
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=
AIRTABLE_PROJECTS_TABLE=Projects

# Dropbox — server-side only, never expose to the browser.
DROPBOX_ACCESS_TOKEN=
